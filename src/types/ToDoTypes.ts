export type ToDoItemType = {
  complete: boolean;
  guid: string;
  text: string;
  visible: boolean;
};

export type ToDoListType = {
  complete: boolean;
  guid: string;
  title: string;
  visible: boolean;
};
